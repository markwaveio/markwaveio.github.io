---
categories: 公众号文章
status:
tags:
aliases:
文章类型:
---
Hermes 最近很热。

越来越多的OpenClaw 用户开始放弃养虾，拥抱爱马仕！

但是，换工具迁移不是一件简单的事情，尤其是你养了很长时间，
各种复杂的配置、模型、skill、记忆能内容，

你已经有自己的记忆、技能、模型配置、消息入口，还有一套跑顺的工作流。  

直接迁移肯定不行！

哪些可以一键迁移？哪些需要手动转换？

## 第一件事：先 `dry-run`

不要一上来就 full migrate。

先跑一次预演。先看 Hermes 准备迁什么，跳过什么，哪里可能出问题。

- 你现在的 OpenClaw 环境有多复杂
- 这次迁移会不会漏掉你最在意的东西


## 第二件事：先保住真正值钱的东西

迁移时，最值钱的不是模型配置。

真正值钱的，通常是这些：

- 记忆
- 技能
- 规则
- 行为习惯
- 工作流

所以迁移时，先看这几类有没有被正确识别：

- `SOUL.md`
- `MEMORY.md`
- 记忆目录
- `AGENTS.md`
- `skills`

如果这些没保住，你迁过去的只是一个新 Agent。  
不是你自己的 Agent。

## 第三件事：单独检查 provider 和密钥

这一块很容易被忽略。

很多人默认，既然官方支持迁移，那模型配置应该也能一起带过去。

现实没这么简单。

provider、环境变量、密钥来源、外部引用方式，都可能出问题。  
文件在，不代表调用真的通。

所以这里不要只看配置有没有迁过去。  
要看 Hermes 能不能真的调起来。

一句话：

**别验证“文件在不在”。要验证“调用通不通”。**

## 第四件事：把 gateway 和消息平台单独处理

这是第二个高频坑。

Telegram、Discord、WhatsApp、网页入口、自动化入口，这些你原来在 OpenClaw 里可能已经接得很顺了。

但迁移后，不一定还能无缝沿用。

有些配置不会完整自动迁。  
有些连接要重新确认。  
有些状态甚至要重新配对。

所以更稳的做法不是一次全搬。

而是分两步：

- 先让 Hermes 跑起来
- 再逐步恢复外围入口

这样出了问题，你知道卡在哪一层。  
不会所有问题堆在一起排。

## 第五件事：迁移后一定要做验证

不要看命令跑完就以为结束了。

很多问题，不出现在迁移过程。  
而出现在迁移后的第一次真实使用。

最少要检查这几项：

- 技能能不能正常加载
- 记忆能不能正常读到
- 模型能不能正常响应
- 关键入口能不能跑通

如果这一步不做，你得到的只是“迁移成功的幻觉”。

## 我对这次迁移的真实判断

Hermes 的迁移门槛不算高。

但它更适合一种用户：

不是想“装个新玩具”的人。  
而是想把现有 Agent 系统，稳定切过去的人。

所以正确顺序应该是：

- 先 `dry-run`
- 先保核心资产
- 单独检查 provider
- 分开处理 gateway 和消息平台
- 迁移后做最小闭环验证

顺序对了，体验会好很多。  
顺序错了，你很容易得到一个“看起来更先进，但用起来更别扭”的 Hermes。

## 一个我很看重的判断

看到这次迁移复杂度之后，我反而更确认一件事：

**统一记忆层很重要。**

我自己做的 `mm3.0` 记忆管家，就是为了解决这件事。

它的目标很明确：

- 在一处管理核心记忆资产
- 通过 MCP 暴露给不同 Agent
- 让多个 Agent 共用同一套核心记忆

如果放到 Hermes 里，我认为最合理的接法是：

- 核心记忆交给 `mm3.0`
- 其他配置继续走 Hermes 官方迁移路径

也就是：

- `memory` 走 mm3.0
- `skills / provider / gateway / messaging` 继续按官方迁移

这样好处很直接：

- 核心记忆不被单个 Agent 绑死
- 多个 Agent 的记忆保持一致
- 迁移时不用把所有历史资产都塞进 Hermes 本地 memory

这个方向，我后面会单独展开写。

## 下一篇我会写什么

下一篇不讲迁移步骤。  
我会直接讲另一个更现实的问题：

**Hermes 虽然更稳、更友好，但有几个坑，是真的会直接劝退人的。**

如果你正准备迁，现在最重要的不是先 full migrate。  
而是先把上面这几步做对。

`Hermes Agent` `OpenClaw` `Agent迁移` `AI工作流` `MCP`


写openclaw迁移Hermes必做的几件配置，帮助大家快速无痛迁移；

- 为什么很多人开始迁移
- 迁移前先想清楚哪几件事
- 真正决定迁移体验的关键配置
- 哪些地方不配好，后面一定出问题

- “这里先不展开，下一篇我专门讲哪些坑最容易劝退”
- “Hermes 真正最强的不是这个配置项，而是它背后的自我进化机制，后面单独写”
- “如果你现在还没决定要不要迁，我最后会再写一篇选择指南”

不管用任何工具，要把自己的重要内容我在自己手上，用mm3.0，mcp，不管openclaw、Hermes、还是未来出现的新工具，都能接入调用，了解你的喜好、你的知识、你的软性技能等





2、Hermes虽然更稳更友好，但几个缺点踩坑也是能直接劝退；
2. 这一篇不要只是列 bug，要写成“谁会被劝退，为什么”。 标题方向： **《Hermes 比 OpenClaw 更稳，但这几个坑不提前知道，真的会劝退》** 核心作用：

- 让你不像站队吹捧
- 建立真实判断感
- 吸引已经在尝试的人留言互动


3、Hermes的核心优势，自我进化能力到底借鉴迁移到所有agent的方案；

